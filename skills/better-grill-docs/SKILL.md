---
name: better-grill-docs
description: Run a grill-with-docs session in a local browser UI: the same rounds and discussion threads as better-grill, and Claude records the glossary (CONTEXT.md) and ADRs as decisions settle.
disable-model-invocation: true
---

Call the Skill tool with `better-grill-base` (listed as `better-grill:better-grill-base` when installed as a plugin) and args `docs`, then follow it. The user's request above is the topic to grill.
